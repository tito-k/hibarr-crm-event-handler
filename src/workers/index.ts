/**
 * Workers Module
 *
 * This module provides information about available background workers.
 * Each worker runs as a standalone process with its own entry point.
 *
 * Available Workers:
 * - crm-webhook.worker.ts: Processes CRM webhook payloads asynchronously.
 *
 * Individual workers:
 * - yarn dev:worker:crm-webhook - Starts the CRM webhook worker in development mode.
 *
 * @module workers
 */

// This file serves as documentation.
// Each worker is a standalone process and should be run independently.
// See package.json scripts for worker management commands.

export {};
